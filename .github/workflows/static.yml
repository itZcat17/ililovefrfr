# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Single deploy job since we're just deploying
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload entire repository
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

         - name: JavaScript action
  # You may pin to the exact commit or the version.
  # uses: silverlyra/script-action@525a406deaf27df3386842620a94760a51287a4e
  uses: silverlyra/script-action@v0.2.2
  with:
    # The script to run (or a script filename)
    script: 
    # Base filename to assign to the script (if inline)
    name: # optional, default is script
    # Input data to make available to your script
    input: # optional, default is 
    # Use "json" to JSON-decode the "input" input, or "string" to expose the raw input string to your function. (default: "json")
    input-encoding: # default is json
    # Use "json" to JSON-encode the script's return value, or "string" to return a string directly. (default: "json")
    result-encoding: # default is json
    # Directory to change into
    cwd: # optional, default is .
    # Create an authenticated GitHub client
    github-token: # optional, default is ${{ github.token }}


          
